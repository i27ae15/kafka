#ifndef CORE_UTILS_H
#define CORE_UTILS_H

#include <iostream>

namespace CoreUtils {

    void printBufferA(const char* buffer, size_t length);
    void printBuffer(const char* buffer, size_t length);
}

#endif // CORE_UTILS_H